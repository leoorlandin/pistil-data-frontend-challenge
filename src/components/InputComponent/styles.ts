import styled from "styled-components";

export const InputFilter = styled.input`
  padding: .5rem;
  border: none;
  margin-right: .5rem;
  border-radius: 6px;
  outline: none;
  
  &::placeholder {
    font-weight: 800;
  }
`